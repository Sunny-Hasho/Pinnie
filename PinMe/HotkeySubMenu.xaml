<Window x:Class="Pinnie.HotkeySubMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HotkeySubMenu" Height="Auto" Width="200"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" SizeToContent="Height"
        Deactivated="Window_Deactivated" FontFamily="Segoe UI">
    <Window.Resources>
        <Style x:Key="SubMenuItemStyle" TargetType="Control">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="Button" BasedOn="{StaticResource SubMenuItemStyle}">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F0F0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="12" BorderThickness="1" BorderBrush="#EEE" Margin="10" Padding="5">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="2" Opacity="0.1"/>
        </Border.Effect>
        <StackPanel>
            <!-- Current Hotkey Display -->
            <TextBlock x:Name="TxtCurrentHotkey" Text="Current: Ctrl + Win + T" 
                       Foreground="#666" Margin="15,10,15,5" FontSize="12"/>

            <!-- Record Button -->
            <Button x:Name="BtnRecord" Content="Change Hotkey..." Click="BtnRecord_Click"/>
            
            <!-- Reset Button -->
            <Button x:Name="BtnReset" Content="Reset to Default" Click="BtnReset_Click" Foreground="#D32F2F"/>
        </StackPanel>
    </Border>
</Window>
