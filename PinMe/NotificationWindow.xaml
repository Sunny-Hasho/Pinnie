<Window x:Class="Pinnie.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notification" Height="Auto" Width="300"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" SizeToContent="Height"
        ResizeMode="NoResize" FontFamily="Segoe UI">
    
    <Border Background="White" CornerRadius="12" BorderThickness="1" BorderBrush="#EEE" Margin="10" Padding="15">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="2" Opacity="0.1"/>
        </Border.Effect>
        <StackPanel Margin="10">
            <!-- Main Message -->
            <TextBlock x:Name="MessageText" Text="Notification Message" 
                       FontSize="15" FontWeight="SemiBold" Foreground="#333" 
                       TextWrapping="Wrap" TextAlignment="Center" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
            

        </StackPanel>
    </Border>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
</Window>
